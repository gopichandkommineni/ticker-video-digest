name: FinTwit API Variance Probe

on:
  workflow_dispatch:
    inputs:
      handle:
        description: "Twitter handle to probe (without @)"
        required: true
        type: string
      since:
        description: "Start date YYYY-MM-DD (default: Jan 1 of current year)"
        required: false
        type: string
      runs:
        description: "Number of repeat runs per provider (default: 3)"
        required: false
        default: "3"
        type: string

# Serialize with other db-writing workflows so probe commits don't race daily-delta.
concurrency:
  group: db-writer
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install project
        run: pip install -e ".[dev]"

      - name: Write .env from secrets
        run: |
          printf 'GETXAPI_KEY=%s\n'        "${{ secrets.GETXAPI_KEY }}"        > .env
          printf 'TWITTERAPI_IO_KEY=%s\n'  "${{ secrets.TWITTERAPI_IO_KEY }}"  >> .env

      - name: Run variance probe
        env:
          PYTHONPATH: .
        run: |
          ARGS="${{ inputs.handle }}"
          [ -n "${{ inputs.since }}" ] && ARGS="$ARGS --since ${{ inputs.since }}"
          ARGS="$ARGS --runs ${{ inputs.runs }}"
          python scripts/run_variance.py $ARGS

      - name: Commit probe outputs to main
        if: always()
        run: |
          # Only commit if new probe files were created.
          git add probes/
          git diff --cached --quiet && echo "No probe outputs to commit — skipping" && exit 0
          git config user.name  "fintwit-bot"
          git config user.email "bot@users.noreply.github.com"
          git commit -m "Probe: variance ${{ inputs.handle }} $(date -u +%Y-%m-%d)"
          git pull --rebase origin main
          git push origin HEAD:main
