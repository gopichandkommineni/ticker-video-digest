---
company: netflix
kind: job_posting          # job_posting | note | talk | blog_post
title: Senior Software Engineer, Media Systems
url: https://jobs.netflix.com/jobs/000000
published_at: 2026-08-14
confidence: high           # high | medium | low — scales how much this counts
---

<!--
This file is an EXAMPLE of the inbox format. It is committed so the shape is
obvious; real inbox files are gitignored.

Drop anything here that you cannot or should not fetch automatically:
  * a posting behind a login or on a referral page
  * notes from a conversation with someone inside the company
  * a talk you watched, summarized in your own words
  * your own considered read of a role

`career ingest --manual` hashes it, stores it in the raw store exactly like a
fetched payload, moves it to manual/processed/, and it flows through the same
normalize → extract → score path as everything else. That is the whole point
of ADR-0006: manual input is an adapter, not an exception.

Content addressing means re-dropping an identical file is a harmless no-op, so
don't be careful about it.

Paste the content below this comment.
-->

We are looking for an engineer to work on the systems that...
