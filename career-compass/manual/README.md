# Manual inbox

Drop files here; run `career ingest --manual`.

See `docs/06-manual-intervention.md` for the format and the reasoning, and
`inbox/EXAMPLE.md` for a template. Processed files move to
`processed/<date>/`; re-dropping an identical file is a harmless no-op.
