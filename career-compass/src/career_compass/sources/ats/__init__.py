"""Applicant-tracking-system adapters.

Almost no large company writes its own job board — they buy one, and the
company's careers page is a JavaScript client calling a public JSON endpoint.
So six adapters here plausibly cover a forty-company watchlist, and adding a
company is a YAML edit (ADR-0004).
"""
